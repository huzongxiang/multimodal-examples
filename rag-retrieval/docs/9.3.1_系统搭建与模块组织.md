# 9.3.1 系统搭建与模块组织

页面级文档问答要处理的，不是普通图像问答，而是页面结构本身仍在表达语义的检索问题。围绕这一点，系统需要同时解决页面对象化、页面与问题的统一编码、多向量存储与重排，以及检索链和回答链的分层协作。

## 1. 系统问题与分层方案

这一案例要处理的，不是普通的图像问答，而是页面结构仍然参与语义表达的文档问答。对于这类任务，标题与正文的相对位置、表格的列对齐关系、图表标题与图例的对应关系，经常和文本内容本身一样重要。也正因为如此，若先把 PDF 压平成 OCR 文本再切块检索，页面中的视觉结构就很容易被削弱。`ColPali` 路线之所以值得单独写一节，正在于它把“页面”直接保留为检索对象，而不是把页面只当作文本提取的中间载体。

围绕这一思想，本项目采用了“本地检索 + 多模态回答”的两层结构。第一层负责页面对象化、页面编码、向量存储与页面重排；第二层负责读取命中页并输出自然语言答案。这样的分工不是简单的工程拆包，而是明确区分了系统中的两个核心问题：一是相关证据页能否被找出来，二是命中页上的内容能否被读懂并表述出来。只要这两个问题被拆开，系统行为就更容易分析，故障来源也更容易定位。

项目中的几个核心模块分别承担如下职责。

1. `PdfManager` 负责把 PDF 页面转换为图像，并保留页面作为后续处理对象。
2. `ColpaliManager` 负责把页面图像和自然语言问题编码到同一多模态检索空间。
3. `MilvusManager` 负责保存页面内部的局部向量，并在召回后执行页面级重排。
4. `Middleware` 负责把索引与检索流程装配成统一接口。
5. `Rag` 负责把命中页送入 `GLM`，完成最终问答。
6. `run_pdf_qa.py` 负责把整条主链收束为可重复执行的脚本入口。

这套系统真正值得关注的，不是模块数量，而是它把“文档页面作为视觉对象进入检索”这件事完整落实到了代码里。

## 2. 运行条件与页面对象化

案例运行依赖三个能力层次：页面渲染、多模态编码和多模态生成。环境与配置代码只是为这三层能力准备运行条件，其本质并不复杂。

环境准备过程可以写成如下形式：

```bash
cd <project_root>
micromamba create -y -n <env_name> python=3.11
micromamba install -y -n <env_name> -c conda-forge poppler
micromamba run -n <env_name> pip install -r requirements.txt
```

配置示例如下：

```dotenv
ZHIPU_API_KEY=<your_api_key>
GLM_MODEL=glm-4.6v-flash
COLPALI_DEVICE=mps
COLPALI_MODEL_PATH=./models/vidore__colpali-v1.2
```

这些配置把检索端与回答端的依赖分开管理，使本地页面级检索和外部生成调用可以分别调试。真正更重要的，是页面在系统入口处就已经被确定为基本处理对象。项目首先通过 `PdfManager` 将 PDF 页面转换为图像：

```python
def save_images(self, id, pdf_path, max_pages, pages: list[int] = None) -> list[str]:
    output_folder = f"pages/{id}/"
    images = convert_from_path(pdf_path, poppler_path=_resolve_poppler_path())

    self.clear_and_recreate_dir(output_folder)

    num_page_processed = 0
    for i, image in enumerate(images):
        if max_pages and num_page_processed >= max_pages:
            break
        if pages and i not in pages:
            continue

        full_save_path = f"{output_folder}/page_{i + 1}.png"
        image.save(full_save_path, "PNG")
        num_page_processed += 1

    return [f"{output_folder}/page_{i + 1}.png" for i in range(num_page_processed)]
```

这段代码的意义并不止于“把 PDF 转成 PNG”。它实际上定义了系统后续的证据单位：不是文本 chunk，不是 OCR 片段，而是保留视觉结构的页面图像。`ColPali` 论文强调的也是这一点，即页面被直接当作视觉检索对象，后续 patch、位置关系和视觉上下文都建立在这个前提之上。

随后，`ColpaliManager` 负责加载 `ColPali` 模型，并把页面与问题映射到同一多模态空间。这里加入了设备选择和本地模型加载逻辑，以适配不同运行环境；但对理解系统主线而言，更关键的是“页面编码”和“问题编码”使用的是同一套 `ColPali` 表示体系。

设备与模型解析逻辑如下：

```python
def _resolve_device(requested_device: str | None = None) -> str:
    if requested_device:
        return requested_device
    env_device = os.getenv("COLPALI_DEVICE")
    if env_device:
        return env_device
    if torch.cuda.is_available():
        return "cuda"
    if torch.backends.mps.is_available():
        return "mps"
    return "cpu"

def _resolve_model_name(default_model_name: str) -> str:
    configured_path = os.getenv("COLPALI_MODEL_PATH")
    if configured_path:
        model_path = Path(configured_path)
        if not model_path.is_absolute():
            model_path = BASE_DIR / model_path
        if model_path.exists():
            return str(model_path)
    default_local_path = BASE_DIR / "models" / "vidore__colpali-v1.2"
    if default_local_path.exists():
        return str(default_local_path)
    return os.getenv("COLPALI_MODEL_NAME", default_model_name)
```

页面编码与问题编码分别由 `process_images()` 和 `process_text()` 完成：

```python
def process_images(self, image_paths:list[str], batch_size=5):
    self._ensure_model_loaded()
    images = self.get_images(image_paths)
    dataloader = DataLoader(
        dataset=ListDataset[str](images),
        batch_size=batch_size,
        shuffle=False,
        collate_fn=lambda x: self.processor.process_images(x),
    )
    ds: List[torch.Tensor] = []
    for batch_doc in dataloader:
        with torch.no_grad():
            batch_doc = {k: v.to(self.model.device) for k, v in batch_doc.items()}
            embeddings_doc = self.model(**batch_doc)
        ds.extend(list(torch.unbind(embeddings_doc.to(self.device))))
    return [d.float().cpu().numpy() for d in ds]

def process_text(self, texts: list[str]):
    self._ensure_model_loaded()
    dataloader = DataLoader(
        dataset=ListDataset[str](texts),
        batch_size=1,
        shuffle=False,
        collate_fn=lambda x: self.processor.process_queries(x),
    )
    qs: List[torch.Tensor] = []
    for batch_query in dataloader:
        with torch.no_grad():
            batch_query = {k: v.to(self.model.device) for k, v in batch_query.items()}
            embeddings_query = self.model(**batch_query)
        qs.extend(list(torch.unbind(embeddings_query.to(self.device))))
    return [q.float().cpu().numpy() for q in qs]
```

这里最关键的不是“得到了一个向量”，而是得到了**一组局部表示**。ColPali 并不把整页压缩成单个粗粒度摘要，而是保留页面内部多个 patch 的表示；问题侧也会形成相应的查询表示。正因为如此，后续检索才不是简单的整页相似度比较，而是能够在页面内部寻找最相关区域的匹配过程。

## 3. 多向量存储与页面级重排

在存储层，`MilvusManager` 保留的是页面内部的局部向量集合，而不是整页单向量摘要。这里关键不在字段命名本身，而在“每一页由多组局部向量构成”这一设计事实。相关实现如下：

```python
schema.add_field(field_name="pk", datatype=DataType.INT64, is_primary=True)
schema.add_field(field_name="vector", datatype=DataType.FLOAT_VECTOR, dim=self.dim)
schema.add_field(field_name="seq_id", datatype=DataType.INT16)
schema.add_field(field_name="doc_id", datatype=DataType.INT64)
schema.add_field(field_name="doc", datatype=DataType.VARCHAR, max_length=65535)
```

真正体现 ColPali 思路的部分，出现在检索后的页面级重排逻辑中：

```python
results = self.client.search(
    self.collection_name,
    data,
    limit=int(50),
    output_fields=["vector", "seq_id", "doc_id"],
    search_params=search_params,
)

doc_vecs = np.vstack(
    [doc_colbert_vecs[i]["vector"] for i in range(len(doc_colbert_vecs))]
)
score = np.dot(data, doc_vecs.T).max(1).sum()
```

这段公式对应的正是 `ColPali` 论文所强调的 `Late Interaction` 机制：查询表示的不同部分分别与候选页面内部的局部向量匹配，再把最优局部匹配结果聚合成页面总分。这样做的结果是，系统既能利用整页作为检索单元，又不必把页面粗暴压缩为一个整体向量。对于标题、表格列名、图表面板标签这类分布在页面不同区域的信息，这种机制比纯文本切块更容易保留局部证据与页面结构之间的关系。

## 4. 流程装配与回答接口

在流程层，`Middleware` 把页面保存、页面编码、向量写入和问题检索装配成一条连续主链：

```python
def index(self, pdf_path: str, id:str, max_pages: int, pages: list[int] = None):
    image_paths = pdf_manager.save_images(id, pdf_path, max_pages)
    colbert_vecs = colpali_manager.process_images(image_paths)

    images_data = [{
        "colbert_vecs": colbert_vecs[i],
        "filepath": image_paths[i]
    } for i in range(len(image_paths))]

    self.milvus_manager.insert_images_data(images_data)
    return image_paths

def search(self, search_queries: list[str]):
    final_res = []
    for query in search_queries:
        query_vec = colpali_manager.process_text([query])[0]
        search_res = self.milvus_manager.search(query_vec, topk=1)
        final_res.append(search_res)
    return final_res
```

这意味着系统在生成答案之前，已经明确完成了页面级证据定位。对于文档问答而言，这一步非常关键，因为它把“找证据”与“读证据”拆成了两个相对独立的阶段。

在生成层，`Rag` 将命中的页面图像与问题一起打包，发送给 `GLM`：

```python
def __get_glm_api_payload(self, query: str, image_paths: List[str]):
    image_payload = []
    for image_path in image_paths:
        base64_image = encode_image(image_path)
        image_payload.append({
            "type": "image_url",
            "image_url": {
                "url": f"data:image/jpeg;base64,{base64_image}"
            }
        })

    payload = {
        "model": os.getenv("GLM_MODEL", "glm-4.6v-flash"),
        "messages": [
            {
                "role": "user",
                "content": [
                    *image_payload,
                    {"type": "text", "text": query}
                ]
            }
        ],
        "max_tokens": 1024
    }
    return payload
```

与原始社区文章中常见的 `Gemini` 或 `GPT-4o` 调用方式相比，这里的回答端替换成了 `GLM`，但系统的结构含义并没有变化：页面级检索负责把证据页找出来，多模态生成负责在命中页上执行阅读与回答。当前接口仍然是“整页图像 + 问题文本”的最小形态，已经足以支撑当前案例，但还没有进入区域级证据裁剪、证据编排和显式引用的阶段。
